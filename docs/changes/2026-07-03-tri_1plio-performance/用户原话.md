# 用户原话

## 2026-07-03（新需求：tri_1plio 上板性能优化）

> $plan-docs 我需要对 tri_1plio 设计做代码性能优化，核心目标是让它在 25625664 大数据量上板运行 时，耗时控制在 28ms 以内，并且上板性能反超原始的 tri 设计。
> 目前我完成了两组测试，出现了不符合预期的性能反转现象：
> 仿真小数据测试：在 256*20 的小数据规模下，通过 emu 跑波形观测，原始 tri 设计的 PL 运行时间为 43.98us，tri_1plio 设计的运行时间为 31.7us，仿真环境中 tri_1plio 速度更快。
> 上板大数据实测：直接上板运行 25625664 的大数据量，原始 tri 设计耗时 30.9ms，而修改参数后的 tri_1plio 设计耗时 33.9ms，反而比原始设计更慢。
> 我不清楚仿真与上板出现性能反转的原因，因此需要帮忙进一步优化 tri_1plio 的代码，排查导致上板速度变慢的瓶颈点，最终让 tri_1plio 在上板场景下的运行速度超过原始 tri 设计，并尽量将耗时优化到 28ms 以内。

## AI 查证结论，非用户原话

- 当前仓库目标目录为 `variants/stencilflow/tri` 与 `variants/stencilflow/tri_1plio`。
- 当前源码中 `tri_1plio` 默认板上规模仍是 `64` 行、`depth=1`、`iter=64`，不是目标 `256*256*64` 的 `iter=16384`。
- 当前 `tri` 与 `tri_1plio` 的轻量 Python 配置测试均存在失败项，说明测试断言与源码状态不一致。

## 2026-07-03（回答 AI 提问：优化边界）

> A
