# 08-测试用例：诊断验证

## 已执行检查

| 命令 | cwd | 结果 | 用途 |
|---|---|---|---|
| `python data/test_sim_semantics.py` | `variants/srad/ours_192lane` | 38 tests OK | 证明 192 Python/映射语义自洽 |
| `python scripts/check_ours_192lane.py` | repo root | OK | 证明项目级 192 快速检查通过 |
| `python data/test_sim_semantics.py` | `variants/srad/ours_96lane` | 24 tests, 7 failures | 发现 96 本地测试老化；不作为板上 96 失败证据 |

## 后续若继续定位，最小验收项

1. 收集 192 硬件编译失败的 first error line、`v++` log path、utilization/timing summary。
2. 比较 96 与 192 的 `v++ link` utilization：AIE-PL shim、AXIS interconnect、NoC、BRAM/LUT/URAM、timing WNS/TNS。
3. 做一个最小 A/B：只改一个变量，例如恢复 stream depth 或关闭 pktmerge，确认失败点是否变化。
4. 若 A/B 仍失败，停止 192 直接扩展，改用 96 稳定基线或中间 lane 数。
